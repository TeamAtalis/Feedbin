<% if @subscription %>
    feedbin.replaceModal("modal-purpose-edit", "<%= j render partial: 'form', locals: {tag_editor: @tag_editor, user_profiles: @user_profiles, orphan_tags: @orphan_tags, subscription: @subscription, app: params[:app].present?} %>");
<% end %>
